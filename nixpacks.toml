[phases.setup]
nixPkgs = ["nodejs-20_x"]
# Forza l'uso di Node.js 20.19+ per compatibilità con Vite 7
[phases.install]
cmds = ["corepack enable"]

[phases.build]
cmds = [
  "cd backend && npm install",
  "cd ../frontend && npm install",
  "cd ../frontend && npx vite build"
]

[start]
cmd = "cd backend && node server.js"
