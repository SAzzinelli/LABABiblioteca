[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "cd backend && npm install",
  "cd ../frontend && npm install",
  "cd ../frontend && npm run build"
]

[start]
cmd = "cd backend && node server.js"
